import styles from './Header.module.scss';

const Header = () => (
  <header className={styles.Header}>
    <h1>Keep it short!</h1>
    <p>make your urls easy to share</p>
  </header>
);

export { Header }